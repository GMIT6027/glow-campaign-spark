
import { EmailConfiguration as EmailConfigComponent } from "@/components/EmailConfiguration";
import { Layout } from "@/components/Layout";

const EmailConfiguration = () => {
  return (
    <Layout>
      <EmailConfigComponent />
    </Layout>
  );
};

export default EmailConfiguration;
